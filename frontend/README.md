# Frontend — React (Vite)

React single-page app for the AI-Powered ETF & Index Investing Simulator.

See the root [README](../README.md) for setup, run, and proxy instructions.

## Scripts

```bash
npm run dev      # dev server at http://localhost:5173
npm run lint     # oxlint
npm run build    # production build to dist/
npm run preview  # preview the production build
```