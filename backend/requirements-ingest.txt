# Local ingestion + FinBERT sentiment stack. NOT installed in the deployed
# container (backend/requirements.txt is the runtime set).
#
# Install locally with:
#     pip install -r requirements-ingest.txt
-r requirements.txt
yfinance>=0.2.54
APScheduler>=3.10,<4
feedparser>=6.0.11
scipy>=1.13
torch>=2.2
transformers>=4.44